# Frontend Routes Configuration

## Purpose
Establishes the URL path mapping, dashboard routes index, and role validation gates.

## Files
- `AppRoutes.jsx`: Central routing registry mapping URLs to views.
- `ProtectedRoute.jsx`: Authentication filter and role access guard component.

## Responsibilities
- Control navigation flow.
- Restrict route viewing permissions.
