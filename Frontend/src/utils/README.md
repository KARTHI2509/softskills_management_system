# Frontend Utilities

## Purpose
Exposes helper methods for parsing and presentation rendering inside pages.

## Files
- `formatters.js`: Helpers to clean display strings, format score numbers, and process timestamps.
- `validators.js`: Client side input forms validators (for register inputs, password safety checks).
